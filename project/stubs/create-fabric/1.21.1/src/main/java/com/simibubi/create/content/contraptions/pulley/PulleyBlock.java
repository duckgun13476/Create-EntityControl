package com.simibubi.create.content.contraptions.pulley;

import net.minecraft.world.level.block.Block;
import net.minecraft.world.level.block.state.BlockBehaviour;

public class PulleyBlock extends Block {
    public PulleyBlock(BlockBehaviour.Properties properties) {
        super(properties);
    }

    public static class RopeBlock extends Block {
        public RopeBlock(BlockBehaviour.Properties properties) {
            super(properties);
        }
    }

    public static class MagnetBlock extends Block {
        public MagnetBlock(BlockBehaviour.Properties properties) {
            super(properties);
        }
    }
}

{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "${mod_authors}"
  ],
  "license": "${mod_license}",
  "icon": "icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "com.Pink_Cats.createentitycontrol.CreateEntityControl"
    ],
    "client": [
      "com.Pink_Cats.createentitycontrol.client.CreateEntityControlClient"
    ]
  },
  "mixins": [
    "createentitycontrol.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "${minecraft_version}",
    "java": ">=${java_version}",
    "fabric-api": ">=${fabric_api_version}",
    "create": "*",
    "forgeconfigapiport": "*"
  }
}
